apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: add-task-ingress
  # annotations:
  #   kubernetes.io/ingress.class: addon-http-application-routing  
spec:
  ingressClassName: nginx
  rules:
  - host: add-task.ak4t7.shop
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: add-task
            port: 
              number: 80
